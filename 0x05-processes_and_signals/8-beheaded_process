#!/usr/bin/env bash
# A Bash script that forcefully kills the 7-highlander process using SIGKILL

# Find the PID of the 7-highlander script
PID=$(pgrep -f "7-highlander")

# Use gdb to send SIGKILL (signal 9) instead of SIGTERM
gdb -q -n -batch -ex "attach $PID" -ex "call (int) kill($PID, 9)" -ex "detach" -ex "quit" >/dev/null 2>&1

